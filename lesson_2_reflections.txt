What happens when you initialize a repository? Why do you need to do it?
1-Create a .git file in directory. 2-.git holds commits 

How is the staging area different from the working directory ant the repository? What value do you think it offers?
1-The staging area is a set up area not a working version. I think it allows you the oppertunity to go over the changes you are about to make.

How can you use the staging area to make sure you have one commit per logical change? 
    1-Using (git diff) this compares working directory and staging area and (git diff --staged) this compares staging area with repository.

What are some situations when branches would be helpful in keeping your history organized? How would from  an exprienced Git user.
1-Working with others and takeing risks without changing master. 

How do the diagrams help you visualize the branch structure?
1-Simple version of commit history.

What are the pros and cons fo Git's automatic merging vs. always doing merges manually?
1- Its great to be able to merge so quicly if working on seperate parts of project. 
